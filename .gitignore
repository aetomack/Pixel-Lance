# Godot-cpp contains platform-specific binaries after running scons platform..
# Don't push .os files generated by scons
# Someone cloning our project will need to run scons to generate binaries for 
# their specific machine.
*.os
*.a
*.lib
bin/*
